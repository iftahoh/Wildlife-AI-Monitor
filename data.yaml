# Path is relative to the repo root (where this file lives).
# Ultralytics resolves it relative to the yaml file location.
path: data/YOLO

train: images/train
val: images/val

names:
  0: fox
  1: gazelle
  2: hyena
  3: porcupine
  4: rabbit